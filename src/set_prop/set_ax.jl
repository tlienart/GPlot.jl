####
#### Axes2D
####

# XXX Axes stuff needs to be all called via things like xlim etc
# so irrelevant to have a set as we wont do axis(... opts...)

# uses a (0,0) centered axis (as opposed to bottom left)
# set_math!(o::Axes2D, v::Bool) = (o.math = ifelse(v, v, ∅); o)
